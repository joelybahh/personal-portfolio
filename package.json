{
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./styles/tailwind.css -o ./dist/tailwind.css --minify && html-minifier --input-dir ./ --output-dir ./dist --file-ext html && cp -R assets/ dist/assets && cp middleware.js dist/ && cp sitemap.xml dist/"
  },
  "dependencies": {
    "@vercel/edge": "^0.1.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "html-minifier": "^4.0.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
